# Array

## What does an array need?

Type - What type of data are we holding in the array
Length - How large of an array do you need
Size - Count of all elements in an array, EXCLUDING NULL/Empty elements
Data - The data that the array is holding
